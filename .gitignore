# Env / secrets
.env
.env.local
**/.env
!**/.env.example

# Node
node_modules
dist
dist-ssr
*.local
npm-debug.log*

# Vercel CLI project link (also gitignored inside division-frontend/;
# root-level link added so `vercel --prod` can run from repo root and
# resolve the Root Directory=division-frontend project setting)
.vercel

# Go
division-backend/tmp
division-backend/bin
*.exe
*.test

# OS / editor
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea

# Local tool configs — contain machine-local secrets (Obsidian REST API key,
# Codex bearer token), not project source. Never commit.
.obsidian
.codex
.claude/settings.local.json
